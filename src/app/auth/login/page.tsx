import { Metadata } from 'next';
import React from 'react'

export const metadata: Metadata = {
    title: "Login",
    description: "Authenticate your account by processing your registered email address and secure password",
};

export default function Login() {
  return (
    <div>Login</div>
  )
}

